# Build output
out/
dist/
*.vsix

# Dependencies
node_modules/

# Wrap It Up runtime data (a user's own wraps / feedback / logs — never published)
.wrap-it-up/

# Local-only working material
writing/

# Launch-video / promo output (large binaries + licensed assets — never published)
brag-output/
*.mp4

# OS / editor cruft
.DS_Store
Thumbs.db
